process {
    executor = 'local' 
    container = 'docker' 
}

docker {
    enabled = true     
    runOptions = '-u $(id -u):$(id -g)' 
}

singularity {
    enabled = false      
}

profiles {
    docker {
        process.container = 'sadigngr/test_lib' 
    }
}

